[project]
name = "logguard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "denyelcnamgyal42-hash", email = "denyelcnamgyal42@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
logguard = "logguard.cli:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
